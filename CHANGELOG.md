# EnhancedFly 更新日志

## 2.2.0

### 跨版本支持

- 支持范围扩展为 Minecraft 1.16.5–1.21.x，面向 Spigot、Paper、Purpur 及其他兼容 Bukkit/Spigot API 的服务端。
- 最低编译基线调整为 Spigot API 1.16.5，`plugin.yml` 使用 `api-version: '1.16'`。
- 服务端运行字节码目标调整为 Java 11；同一 JAR 可运行在 Java 11、17、21 等更高版本 JVM 上。
- 移除主程序中的 Java 16 模式匹配语法，不引入 NMS、CraftBukkit 或特定服务端私有 API。
- 启用日志使用构建版本号，不再显示硬编码的旧版本。

### 验证

- 20 项自动化测试全部通过。
- `verifyPluginJar` 检查成品中的 API 基线和 Java 11 字节码兼容性。
- MySQL Driver、HikariCP、SQLite Driver 和 SQLite 实际读写打包检查通过。
- 已在 Paper 1.16.5、Paper 1.20.1、Paper 1.21.11 和 Purpur 1.21.11 上完成无玩家启动、插件启用、SQLite 初始化与正常停服验证。
- 玩家余额、计费、购买、恢复快照和停服保存逻辑没有改变。

### 实际边界

最低编译基线和启动验证不能代替真实玩家集成测试。仍需在实际服务器插件组合中验证飞行、Vault 商店、权限和真实 MySQL 行为。

## 2.1.1

本次以当前源码为依据进行了修复。旧审查仅用作线索，没有将推测直接当成漏洞。项目现已纳入 Git 版本管理，旧源码可从初始发布前的本地历史或 GitHub 版本记录追踪。

## 数据与存储

- 玩家数据加载完成前不再创建可被消费、购买或保存的默认对象。加载结果在服务器主线程接入，并校验登录会话，防止退服、重连后的旧回调覆盖新状态。
- 数据库读取失败与“数据库没有该玩家”分开处理；初始化或建表失败会阻止插件正常启用，不再静默改用另一套空数据。
- 文件与数据库 IO 使用插件自己的顺序队列；保存提交独立快照，避免工作线程读取仍在变化的玩家对象和乱序覆盖。SQLite 单连接的操作、维护任务也进入同一队列。
- 玩家余额和统计表在同一个数据库事务中保存；移除了监听器额外执行的统计增量写入，避免与全量快照重复计数。
- 数据库写入前保存绝对值恢复快照，成功后删除；失败时保留在 `plugins/EnhancedFly/recovery/mysql` 或 `recovery/sqlite`，下次加载时读取并重试保存。不要删除尚有内容的恢复目录。
- YAML 使用临时文件替换，损坏的数据文件明确报错。正常退出只保存该玩家，成功后清理其活动缓存；周期保存不再每秒执行全量 IO，使用 `statistics.save-interval`（至少 10 秒，默认 300 秒）。文件模式仍会在内存中保留 YAML 数据树。
- 停服先结束飞行计时，再提交最终快照，等待存储队列完成后关闭数据库。存储错误会记录日志，不再吞掉异常。
- SQLite 成就加载和保存接入现有接口；成就合并回到主线程，并防止过期登录回调写入。
- 动态统计字段使用白名单；飞行时间拒绝负数、零增量和整数溢出。

## 飞行行为

- 开启飞行统一检查数据就绪、使用权限、世界限制和飞行资源。`enhancedfly.free` 只免除资源消耗，世界豁免仍由 `enhancedfly.bypass.world` 控制。
- `/fly <玩家> on` 也遵守目标玩家的飞行条件，不再产生没有时间却能持续飞行的状态。需要授予资源时仍使用 `/flyadmin give`。
- 只记录、计费和撤销本插件接管的飞行；创造、旁观模式不消耗余额，也不会被清除原生飞行能力。检测到其他功能已提供飞行时，不主动接管。
- 使用单调时钟记录实际飞行时间，每 tick 检查。**每次连续飞行按开始的秒向上取整计费**，暂停和离线不持续扣费；反复进行不足一秒的飞行也不能免费使用。
- 命令开启的飞行同样计入统计。传送、被取消的移动和单纯转头不再计入飞行距离；统计总开关实际生效。
- 进入禁飞世界、游戏模式变更、死亡、退出和插件停用时处理所属状态。退出与恢复开关通过玩家持久化标记关联，不再仅凭“还有余额”自动开启。
- 插件撤销正在进行的飞行时，提供最长 10 秒、一次落地伤害保护。
- 恢复原先飞行速度；配置速度、粒子数量和动作栏周期限制在有效范围，带额外参数的粒子类型回退为 CLOUD。

## 商店、成就与配置

- GUI 使用专属 `InventoryHolder` 标识，支持自定义标题，不再通过中英文标题片段识别。
- 阻止拖拽、下方背包、Shift、数字键等操作误触发购买或移动菜单物品。有效点击延迟到下一 tick 执行，检查菜单仍然打开，并合并同一菜单的待处理点击。
- 页码绑定实际菜单；共享商店实例，修复重新构造成就界面导致翻页状态丢失的问题。空列表显示至少一页。
- 点击时重新检查商店开关、访问权限、玩家数据、价格与时长。拒绝非有限或负价格、无效时长、时间溢出；永久用户不再继续购买无用途的临时时间。
- 经济系统不可用时余额检查返回失败；实际扣款失败会提示。购买次数包含在保存快照内。
- 在购买和查询限额时检查跨日，不再依赖每小时任务才能刷新额度。
- 重载同步更新语言、商店、成就展示、经济接入和飞行定时任务，并关闭旧菜单。数据库连接配置需要重启后生效。
- 修复中英文语言文件中的重复 YAML 键、缺失默认文本回退、重复消息前缀、无效成就图标和通知开关未生效。
- 默认“3 分钟”商品的 `duration` 从 300 改为 180，说明文字改为实际飞行计费。已有服务器的 `config.yml` 不会被默认资源覆盖。
- 旧 `settings.time-check-interval` 和 `worlds.disable-on-world-change` 键继续允许存在，但不再用于延迟计费检查或允许在禁飞世界保留插件飞行。

## 验证结果

执行：`./gradlew.bat test verifyPluginJar --console=plain`，Java 17，Gradle Wrapper 8.14。

- 20 项自动化测试：数据加载与重连竞争、失败读取、快照隔离与顺序、SQLite 事务失败回滚及待保存数据恢复、最终保存排空、统计和成就往返、时间溢出、损坏 YAML、世界权限、创造模式、短飞行计费、GUI 拖拽/下方背包/重复点击/关闭后的点击等。
- 独立类加载器加载成品 JAR，验证重定位后的 MySQL 驱动、资源文件、HikariCP 和 SLF4J，并通过打包的 SQLite 原生驱动完成实际建表、写入和读取。
- 成品：`build/libs/EnhancedFly-2.1.1.jar`。SQLite JNI 包路径保留，合并服务描述文件，避免数据库驱动打包后无法发现。

Bukkit 库存点击中延迟打开或关闭界面的依据：[InventoryClickEvent 官方文档](https://hub.spigotmc.org/javadocs/bukkit/org/bukkit/event/inventory/InventoryClickEvent.html)。

## 实际边界

没有运行 Mohist 1.20.1 实服、多玩家压力测试或真实 Vault/MySQL 联调。自动化测试使用真实 SQLite，玩家与 Bukkit 调度使用模拟对象；不能据此宣称所有模组和其他飞行插件组合都兼容。

Vault 扣款与本插件的飞行数据、限购文件不是同一个数据库事务。顺序保存、恢复快照和正常停服等待降低了丢失风险，但进程强杀或断电发生在跨系统写入之间时，仍不能保证扣款和发货完全原子化；最后一次尚未提交保存的内存计时也可能丢失。当前实现面向单台服务器，不提供多服共享余额的分布式并发控制。

`enhancedfly.use` 默认开放、按实际飞行消耗时间、数据库密码位于本地配置文件这些设计本身没有被当成漏洞改掉。

替换插件应在正常停服后进行，保留原服务器数据、配置和恢复目录，使用新 JAR 替换旧 JAR，避免同时放入两个版本。Git 源码历史不是服务器玩家数据备份。
